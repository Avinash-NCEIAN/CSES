#include<bits/stdc++.h>
using namespace std;

int main()
{
	int n;
	cin>>n;

	while(n-->0) {
	    int num;
	    cin>>num;
		int  moves = 0;
		for(int i = 1; i*i<=num; i++) {
			if(num%i == 0) {
				moves++;
				if(i != num/i)moves++;
			}
		}

		cout<<moves<<endl;
	}

	return 0;
}
